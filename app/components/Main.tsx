import About from './About';

const Main = () => {
  return (
    // Render different sections here depending on the route, but default to About
    <div>
      <About />
    </div>
  );
};

export default Main;
